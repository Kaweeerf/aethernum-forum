import React from 'react';export default function Thread({thread,onBack}){return (<div><button onClick={onBack} className='muted'>← Back</button><div className='card' style={{marginTop:12}}><h2>{thread.title}</h2><div className='muted'>{thread.meta}</div></div>{thread.posts.map((p,i)=>(<div className='card' key={i} style={{marginTop:8}}><div style={{fontWeight:700}}>{p.author} <span className='muted' style={{fontSize:12}}>{p.time}</span></div><div style={{marginTop:6}}>{p.content}</div></div>))}</div>)}