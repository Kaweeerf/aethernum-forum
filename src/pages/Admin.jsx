import React,{useState} from 'react';import sample from '../seed/sampleThreads.json';export default function Admin({onBack}){const [threads,setThreads]=useState(sample);return (<div><button onClick={onBack} className='muted'>← Back</button><h2>Admin Panel</h2>{threads.map(t=>(<div className='card' key={t.id} style={{marginTop:8}}><div style={{display:'flex',justifyContent:'space-between'}}><div><div style={{fontWeight:700}}>{t.title}</div><div className='muted'>{t.meta}</div></div><div><button onClick={()=>{if(confirm('Purge?')) setThreads(threads.filter(x=>x.id!==t.id))}}>Purge</button></div></div></div>))}</div>)}