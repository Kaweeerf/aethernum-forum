import React,{useState} from 'react';import Forum from './pages/Forum';import Thread from './pages/Thread';import Admin from './pages/Admin';export default function App(){const [route,setRoute]=useState('forum');const [thread,setThread]=useState(null);return (<div><header style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><h1 style={{margin:0}}>Aetherum Forum</h1><nav><button onClick={()=>setRoute('forum')}>Forum</button> <button onClick={()=>setRoute('admin')}>Admin</button></nav></header><main style={{marginTop:16}}>{route==='forum' && <Forum onOpen={(t)=>{setThread(t);setRoute('thread')}}/>}{route==='thread' && thread && <Thread thread={thread} onBack={()=>setRoute('forum')}/>} {route==='admin' && <Admin onBack={()=>setRoute('forum')}/>}</main></div>);}